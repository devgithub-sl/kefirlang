import { defineConfig } from 'vite'

export default defineConfig({
  // REPLACE 'kefir-lang' WITH YOUR ACTUAL REPO NAME
  // Example: If your repo is https://github.com/user/my-compiler
  // Then use base: '/my-compiler/'
  base: 'https://github.com/devgithub-sl/kefirlang.git' 
})